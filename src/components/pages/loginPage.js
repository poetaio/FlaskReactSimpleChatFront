import React from "react";
import Login from "../login";

function LoginPage() {
    return (
        <div className="main-wrapper">
            <Login/>
        </div>
    );
}

export default LoginPage;
